# Shared UI assets

Place reusable packaged icons, fonts, and images in `icons/`, `fonts/`, and `images/`. Components reference assets through QML module/resource paths rather than relative parent traversal.
