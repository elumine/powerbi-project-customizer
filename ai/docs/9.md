- general ui
  - left sidebar panel should be 50% of total width
  - json text editor is about 40%
    - if json text is too large, it should be horizontally scrolled
  - left sidebar with button is about 10%

- json editor
  - ability to find and replace by CTRL+F in json text editor
  - we should display lines of text file as in Visual Studio Code editor

- search and replace
  - search should show matches as list, each list items is "[file] [line] [text]"

- visual editor
  - controls should be unique, not just text
  - we should have: color = ColorPicker, boolean = checkbox, opacity = slider, text = textfield, numbers = numeric input

- left sidebar
  - buttons should have icons as in Visual Studio Code

- marcos panel
  - each marcos should have expandable list steps to explain to user what it does exactly