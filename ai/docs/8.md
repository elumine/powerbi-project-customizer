- add 3rd dynamic filter by visual name
- for each control
  - description of controlled property under title
  - control should have default value = first match value
  - exapndeble list of existing matches in visual.json files as unordered list (collapsed by default), with each list item "[{file}] [{line}] {property}: {value}" under edit box
- visual editor controls for specific visual type should mirror json structure and be hierarchial, example:
  - position
    - x
    - y
    - z
    - width
    - etc...

    
- bugs
  - history should not reset scroll on clicking on history item
  - fix bug with search, currently when i click on up/down buttons it scrolls not to line of match but to random line